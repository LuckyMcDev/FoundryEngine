# Configuration

The configuration of a bundle is the
`${bundle name}.bundles.toml` file located in the `src/main/resources` directory of your bundle project.
This file contains important information about your bundle, such as its name, version, description, and author.