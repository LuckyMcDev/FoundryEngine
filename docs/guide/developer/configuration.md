# Configuration

The `bundlename.bundles.toml` File contains important information about your bundle, such as its name, version,
description, and author.
The `gradle.properties` file also contains some of this information, and the content of both files MUST match for the
bundle to be built successfully.