{
	"scripts": {
		"docs:dev": "vitepress dev . --host",
		"docs:build": "vitepress build . --no-check-dead-links",
		"docs:preview": "vitepress preview ."
	},
	"devDependencies": {
		"vitepress": "^1.6.4"
	}
}