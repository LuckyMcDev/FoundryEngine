# Bundle Config

> **⚠️ Bundle configuration is currently non-functional.** The `BundleConfigSpec` API exists in the codebase but does not work correctly at runtime. Configuration values cannot be reliably persisted or loaded. This system is planned for a future rework — do not rely on it for now.
